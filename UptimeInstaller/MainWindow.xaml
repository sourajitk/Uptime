<Window x:Class="UptimeInstaller.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Uptime Taskbar App Setup" 
        Width="540" Height="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <!-- Modern Scrollbar Styling for EULA -->
        <Style x:Key="ModernScrollViewer" TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid x:Name="Grid" Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" Grid.Column="0" Margin="{TemplateBinding Padding}" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" CanContentScroll="{TemplateBinding CanContentScroll}"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar" Grid.Column="1" AutomationProperties.AutomationId="VerticalScrollBar" Cursor="Arrow" Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" Minimum="0" Maximum="{Binding ScrollableHeight, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Width="8"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2D2D2D" />
            <Setter Property="Foreground" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#444444" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#3D3D3D" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#555555" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#1E1E1E" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#222222" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter TargetName="border" Property="Background" Value="#1D1D1D" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#2D2D2D" />
                                <Setter Property="Foreground" Value="#666666" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Accent Button Style -->
        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#0078D4" />
            <Setter Property="BorderBrush" Value="#0078D4" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#1A86DF" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#1A86DF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#005A9E" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#005A9E" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter TargetName="border" Property="Background" Value="#1D1D1D" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#2D2D2D" />
                                <Setter Property="Foreground" Value="#666666" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TitleBar Buttons -->
        <Style x:Key="TitleBarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#CCCCCC" />
            <Setter Property="Width" Value="46" />
            <Setter Property="Height" Value="32" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#2E2E2E" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#1E1E1E" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Close Button (Red Hover) -->
        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource TitleBarButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#E81123" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="border" Property="Background" Value="#F1707A" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Checkbox Style -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E0E0E0" />
            <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <!-- Modern Textbox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#202020" />
            <Setter Property="Foreground" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#444444" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#666666" />
                            </Trigger>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#0078D4" />
                                <Setter TargetName="border" Property="Background" Value="#1A1A1A" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Main Window Border Frame with shadow & rounded corners -->
    <Border CornerRadius="8" Background="#181818" BorderBrush="#333333" BorderThickness="1" SnapsToDevicePixels="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="64"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Border Grid.Row="0" Background="#1E1E1E" CornerRadius="8,8,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center">
                        <Path Data="M 12 2 C 6.48 2 2 6.48 2 12 C 2 17.52 6.48 22 12 22 C 17.52 22 22 17.52 22 12 C 22 6.48 17.52 2 12 2 Z M 12.5 13 H 7 V 11.5 H 11 V 7 H 12.5 V 13 Z" 
                              Fill="#0078D4" Width="14" Height="14" Stretch="Uniform" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Uptime Taskbar App Setup" Foreground="#AAAAAA" FontSize="11.5" FontFamily="Segoe UI Variable Text, Segoe UI" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Window Control Buttons -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Style="{StaticResource TitleBarButton}" Click="Minimize_Click" Focusable="False">
                            <TextBlock Text="⎯" FontSize="9" FontWeight="Bold"/>
                        </Button>
                        <Button Style="{StaticResource CloseButton}" Click="Close_Click" Focusable="False">
                            <TextBlock Text="✕" FontSize="10"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Wizard Page Container -->
            <Grid Grid.Row="1" Margin="24,16,24,8">

                <!-- Page 1: Welcome Page -->
                <Grid x:Name="PageWelcome" Visibility="Visible">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <!-- Centered stylized dynamic vector clock icon -->
                        <Grid Width="72" Height="72" HorizontalAlignment="Center" Margin="0,0,0,16">
                            <Ellipse Fill="#1F2A38" Stroke="#0078D4" StrokeThickness="3.5" Width="72" Height="72"/>
                            <!-- Hands -->
                            <Line X1="36" Y1="36" X2="52" Y2="36" Stroke="#0078D4" StrokeThickness="3.5" StrokeEndLineCap="Round"/>
                            <Line X1="36" Y1="36" X2="36" Y2="18" Stroke="#0078D4" StrokeThickness="3.5" StrokeEndLineCap="Round"/>
                        </Grid>
                        
                        <TextBlock x:Name="WelcomeTitle" Text="Welcome to Uptime Setup" Foreground="#FFFFFF" FontSize="22" FontWeight="Bold" FontFamily="Segoe UI Variable Display, Segoe UI" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                        <TextBlock x:Name="WelcomeDescription" Text="Easily check your computer's uptime straight from your taskbar!" 
                                   Foreground="#CCCCCC" FontSize="13.5" FontFamily="Segoe UI Variable Text, Segoe UI" TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center" LineHeight="20"/>
                    </StackPanel>

                    <TextBlock Grid.Row="1" Text="Click Next to continue." x:Name="WelcomeFooter" Foreground="#888888" FontSize="12.5" FontFamily="Segoe UI Variable Text, Segoe UI" Margin="0,0,0,8" HorizontalAlignment="Center"/>
                </Grid>

                <!-- Page 2: License Agreement -->
                <Grid x:Name="PageLicense" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="License Agreement" Foreground="#FFFFFF" FontSize="18" FontWeight="Bold" FontFamily="Segoe UI Variable Text, Segoe UI" Margin="0,0,0,8"/>
                    
                    <Border Grid.Row="1" Background="#111111" BorderBrush="#333333" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,12">
                        <TextBox Background="Transparent" BorderThickness="0" Foreground="#CCCCCC" FontFamily="Consolas, Courier New" FontSize="11"
                                 IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" AcceptsReturn="True"
                                 Text="Apache License&#x0a;Version 2.0, January 2004&#x0a;http://www.apache.org/licenses/&#x0a;&#x0a;Licensed under the Apache License, Version 2.0 (the &quot;License&quot;);&#x0a;you may not use this file except in compliance with the License.&#x0a;You may obtain a copy of the License at&#x0a;&#x0a;    http://www.apache.org/licenses/LICENSE-2.0&#x0a;&#x0a;Unless required by applicable law or agreed to in writing, software&#x0a;distributed under the License is distributed on an &quot;AS IS&quot; BASIS,&#x0a;WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.&#x0a;See the License for the specific language governing permissions and&#x0a;limitations under the License."/>
                    </Border>

                    <CheckBox Grid.Row="2" x:Name="ChkAcceptLicense" Checked="ChkAcceptLicense_Changed" Unchecked="ChkAcceptLicense_Changed" Style="{StaticResource ModernCheckBox}">
                        <TextBlock Text="I accept the terms of the Apache-2.0 License" Foreground="#CCCCCC" Margin="6,0,0,0"/>
                    </CheckBox>
                </Grid>

                <!-- Page 3: Folder Selection -->
                <Grid x:Name="PageFolder" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Select Installation Folder" Foreground="#FFFFFF" FontSize="18" FontWeight="Bold" FontFamily="Segoe UI Variable Text, Segoe UI" Margin="0,0,0,8"/>

                    <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="0,0,0,16">
                        <TextBlock Text="Install folder:" Foreground="#AAAAAA" Margin="0,0,0,6" FontSize="13"/>
                        
                        <Grid Margin="0,0,0,24">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" x:Name="TxtInstallPath" Style="{StaticResource ModernTextBox}" Height="34" VerticalContentAlignment="Center"/>
                            <Button Grid.Column="1" Content="Browse..." Click="BrowseFolder_Click" Style="{StaticResource ModernButton}" Margin="8,0,0,0" Height="34"/>
                        </Grid>

                        <CheckBox x:Name="ChkStartup" IsChecked="True" Style="{StaticResource ModernCheckBox}" Margin="0,0,0,12">
                            <TextBlock Text="Run automatically on Windows startup" Foreground="#CCCCCC" Margin="6,0,0,0"/>
                        </CheckBox>
                    </StackPanel>
                </Grid>

                <!-- Page 4: Installing Page -->
                <Grid x:Name="PageInstalling" Visibility="Collapsed">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Installing Uptime..." Foreground="#FFFFFF" FontSize="20" FontWeight="Bold" FontFamily="Segoe UI Variable Text, Segoe UI" Margin="0,0,0,8" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="TxtStatus" Text="Extracting files..." Foreground="#AAAAAA" FontSize="13.5" Margin="0,0,0,24" HorizontalAlignment="Center"/>
                        
                        <ProgressBar x:Name="InstallProgressBar" Minimum="0" Maximum="100" Value="20" Height="6" BorderThickness="0" Background="#222222" Foreground="#0078D4">
                            <ProgressBar.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="3"/>
                                </Style>
                            </ProgressBar.Resources>
                        </ProgressBar>
                    </StackPanel>
                </Grid>

                <!-- Page 5: Completed Page -->
                <Grid x:Name="PageCompleted" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <!-- Success Checkmark SVG style -->
                        <Grid Width="72" Height="72" HorizontalAlignment="Center" Margin="0,0,0,16">
                            <Ellipse Fill="#163820" Stroke="#107C41" StrokeThickness="3.5" Width="72" Height="72"/>
                            <Path Data="M 28 38 L 34 44 L 48 28" Stroke="#107C41" StrokeThickness="4" StrokeEndLineCap="Round" StrokeStartLineCap="Round"/>
                        </Grid>
                        
                        <TextBlock Text="Installation Completed" Foreground="#FFFFFF" FontSize="22" FontWeight="Bold" FontFamily="Segoe UI Variable Display, Segoe UI" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                        <TextBlock Text="The Uptime Taskbar App has been successfully installed on your computer." 
                                   Foreground="#CCCCCC" FontSize="13.5" FontFamily="Segoe UI Variable Text, Segoe UI" TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center"/>
                    </StackPanel>

                    <CheckBox Grid.Row="1" x:Name="ChkLaunchOnFinish" IsChecked="True" Style="{StaticResource ModernCheckBox}" HorizontalAlignment="Center" Margin="0,0,0,12">
                        <TextBlock Text="Launch Uptime Taskbar App now" Foreground="#CCCCCC" Margin="6,0,0,0"/>
                    </CheckBox>
                </Grid>

            </Grid>

            <!-- Bottom Navigation Bar -->
            <Border Grid.Row="2" Background="#1A1A1A" BorderBrush="#333333" BorderThickness="0,1,0,0" CornerRadius="0,0,8,8">
                <Grid Margin="24,0,24,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left button: Back -->
                    <Button Grid.Column="0" x:Name="BtnBack" Content="Back" Click="Back_Click" Style="{StaticResource ModernButton}" Width="90" Height="34" VerticalAlignment="Center" Visibility="Collapsed"/>
                    
                    <!-- Right buttons: Next / Cancel / Install / Finish -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="BtnCancel" Content="Cancel" Click="Cancel_Click" Style="{StaticResource ModernButton}" Width="90" Height="34" Margin="0,0,8,0"/>
                        <Button x:Name="BtnNext" Content="Next" Click="Next_Click" Style="{StaticResource AccentButton}" Width="90" Height="34"/>
                    </StackPanel>
                </Grid>
            </Border>

        </Grid>
    </Border>
</Window>
